import React from 'react';

const LoginForm = () => <div>login component</div>;

// LoginForm.propTypes = {
//   handleSubmit: PropTypes.func.isRequired,
//   pristine: PropTypes.bool.isRequired,
//   submitting: PropTypes.bool.isRequired,
// };

export default LoginForm;
